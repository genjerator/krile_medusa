# STOREFRONT_PLANETA_DOMAIN → Planeta storefront
# TODO: Replace STOREFRONT_PLANETA_DOMAIN with the actual domain
# NOTE: SSL blocks commented out until domains are ready.
#       Run nginx/init-ssl.sh first, then uncomment the HTTPS server block.

server {
    listen 80;
    server_name STOREFRONT_PLANETA_DOMAIN;

    # location /.well-known/acme-challenge/ {
    #     root /var/www/certbot;
    # }

    location / {
        proxy_pass         http://storefront-planeta:3000;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# ─── Enable after running init-ssl.sh ────────────────────────────────────────
# server {
#     listen 443 ssl;
#     server_name STOREFRONT_PLANETA_DOMAIN;
#
#     ssl_certificate     /etc/letsencrypt/live/STOREFRONT_PLANETA_DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/STOREFRONT_PLANETA_DOMAIN/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     location / {
#         proxy_pass         http://storefront-planeta:3000;
#         proxy_http_version 1.1;
#         proxy_set_header   Upgrade $http_upgrade;
#         proxy_set_header   Connection 'upgrade';
#         proxy_set_header   Host $host;
#         proxy_set_header   X-Real-IP $remote_addr;
#         proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
