services:
  medusa:
    build: .
    ports:
      - "9000:9000"
    env_file:
      - .env.aws
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped
